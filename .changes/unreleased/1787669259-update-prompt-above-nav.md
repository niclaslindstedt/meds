---
type: Fixed
title: Update prompt clears the bottom bar
---

The "a new version is ready" prompt now sits above the bottom navigation instead of on top of it, so the four tabs stay reachable while the prompt is up.
